i# ACEest Fitness & Gym — DevOps Assignment

## Overview
This repo contains a Flask API adapted from ACEest desktop code, with CI/CD pipelines using GitHub Actions and Jenkins.

## Setup
```bash
git clone https://github.com/vk-c0de/aceest-fitness.git
cd aceest-fitness
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python app/app.py

